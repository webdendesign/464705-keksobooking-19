'use strict';

// var count = 8;

// var createElement = function () {
//   var element = [];

//   for (var i = 0; i < count; i++) {
//     element.push({

//     })
//   }
// // }

// var map = document.querySelector('.map');
// map.classList.remove('map--faded');

// var template = document.querySelector('#template').content.querySelector('.map__pin');

// var mapPins = document.querySelector('.map__pins');

// for (var i = 0; i < usersAds.length; i++) {
//   var userElement = template.cloneNode(true);

//   userElement.querySelector('.map__pin').style.left = usersAds[i].location.x;
//   userElement.querySelector('.map__pin').style.top = usersAds[i].location.y;


//   userElement.querySelector('img').src = usersAds[i].author;

//   userElement.querySelector('img').alt = usersAds[i].title;

//   mapPins.appendChild(userElement);

// }

// var typeHouse = function (type) {
//     if (type === 'palace') {
//         return 'Дворец';
//     } else if (type === 'flat') {
//         return 'Квартира';
//     } else if (type === 'bungalo') {
//         return 'Бунгало';
//     } else { return 'Дом' };
// };

// mapPinMain.addEventListener('mouseup', function (evt) {
//     evt.preventDefault;
//     window.form.activForm();
//     window.pin.renderPins(window.data);
//     // window.card.renderCards(window.data);
//     mapPin.classList.remove('hidden');
//     // mapPins.appendChild(fragmentPin);
//   });
